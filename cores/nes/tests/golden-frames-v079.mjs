import fs from 'node:fs';import path from 'node:path';import {Cartridge} from '../js/cartridge.js';import {Bus} from '../js/bus.js';
function fnv(arr){let h=0x811c9dc5;const u=new Uint8Array(arr.buffer,arr.byteOffset,arr.byteLength);for(const v of u){h^=v;h=Math.imul(h,0x01000193)}return (h>>>0).toString(16).padStart(8,'0')}
const expected={'batman-the-video-game-usa.nes':'d5129a6e','contra-force-usa.nes':'3dde1dc5','ducktales-usa.nes':'11e41e26','teenage-mutant-ninja-turtles-usa.nes':'449a818f','tiny-toon-adventures-usa.nes':'a2cc17f2','mighty-final-fight-usa.nes':'893f0d77','1942-japan-usa.nes':'5c3809bb','spider-man-return-of-the-sinister-six-usa.nes':'3dde1dc5','paperboy-usa.nes':'d325f531','kart-fighter.nes':'9c5addc5','double-dragon-iii-the-sacred-stones-usa.nes':'3dde1dc5','bubble-bobble-usa.nes':'b4dddedf'};
for(const [f,want] of Object.entries(expected)){const b=fs.readFileSync(path.join('roms',f)),bus=new Bus();bus.insertCartridge(new Cartridge(b.buffer.slice(b.byteOffset,b.byteOffset+b.byteLength)));bus.reset();for(let frame=0;frame<120;frame++){bus.ppu.frameComplete=false;let g=0;while(!bus.ppu.frameComplete&&g++<200000)bus.clock();if(!bus.ppu.frameComplete)throw new Error(`${f}: watchdog`)}const got=fnv(bus.ppu.image);if(got!==want)throw new Error(`${f}: ${got} != ${want}`);console.log('OK golden',f,got)}
