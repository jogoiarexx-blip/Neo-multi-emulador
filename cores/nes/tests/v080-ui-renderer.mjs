import assert from 'assert';import fs from 'fs';import path from 'path';import {fileURLToPath} from 'url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const html=fs.readFileSync(path.join(root,'index.html'),'utf8'),main=fs.readFileSync(path.join(root,'js/main.js'),'utf8'),vr=fs.readFileSync(path.join(root,'js/video-renderer.js'),'utf8');for(let i=1;i<=10;i++)assert(html.includes(`value="${i}"`));assert(html.includes('patternTable0')&&html.includes('nametableViewer')&&html.includes('oamViewer'));assert(main.includes('shiftStateSlot')&&main.includes('WebGLPresenter'));assert(vr.includes("getContext('webgl2'"));console.log('v0.8.0 UI/save/GPU renderer ok');