# Runtime local EmulatorJS

O NEO GB procura primeiro `stable-4.2.3/data/loader.js`. Execute `scripts/install-runtime.ps1` no Windows ou `scripts/install-runtime.sh` em Unix para preencher esta pasta. Se o runtime local não existir, o frontend usa o CDN configurado como fallback.
